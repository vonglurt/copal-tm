[package]
name = "copal-tm-tty"
description = "Terminal layer for Copal Task Manager: raw mode, a damage-tracked cell canvas, key decoding"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
# The workspace README is the project's front page, and crates.io shows a crate
# with no readme as a blank one. Cargo copies it into the package at publish.
readme = "../../README.md"

[dependencies]
